# BioValue-AI 创新药市值评估智能体系统

> 基于 Go 编排 + Python LLM 的工业级 Agent 工作流框架

## 📋 项目概述

BioValue-AI 是一个自动化的创新药市值评估系统，通过协调 7 个垂直领域的 AI Agent，完成对指定创新药公司的全生命周期价值评估与市值预测。

### 核心特性

- 🔄 **Temporal 工作流编排**: 强一致性、故障恢复、状态持久化
- 🤖 **多 Agent 协同**: 财务审计、管线扫描、临床评估、市场分析、估值计算
- 🔧 **MCP 工具调用**: 动态发现与调用外部能力（Web Search、PDF Parser、Code Sandbox）
- 📊 **ReAct 范式**: Reason -> Act -> Observe 循环
- 💾 **智能缓存**: Redis 缓存 + RAG 机制

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                     Client Layer                             │
│                  (REST/gRPC API Gateway)                     │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                  Orchestration Layer (Go)                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   Temporal  │  │  Workflow   │  │  Activity Workers   │  │
│  │   Server    │  │  Workers    │  │  (6 Agents)         │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                Intelligence Layer (Python)                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ LLM Bridge  │  │ MCP Client  │  │ Embedding Service   │  │
│  │  (gRPC)     │  │             │  │                     │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                     Storage Layer                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │    Redis    │  │   Milvus    │  │    PostgreSQL       │  │
│  │   (Cache)   │  │ (Vector DB) │  │   (Temporal)        │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 🤖 Agent 工作流

| Agent | 职责 | 输入 | 输出 |
|-------|------|------|------|
| **A1: Financial Auditor** | 财务健康度分析 | 年报 PDF | 现金跑道、健康评分 |
| **A2: Pipeline Scout** | 管线全景扫描 | 公司 URL | 结构化管线数据 |
| **A3: Clinical Assessor** | 临床竞争力分析 | 管线列表 | POS、竞品对比 |
| **A4/A5: Market Strategist** | 市场与 BD 预测 | 管线 + 临床数据 | TAM、BD 估值 |
| **A6: Report Generator** | 研报生成 | 全部数据 | Markdown 报告 |
| **A7: Valuation Actuary** | 动态市值建模 | 财务 + 市场数据 | rNPV 估值 |

## 🚀 快速开始

### 前置要求

- Go 1.22+
- Python 3.11+
- Docker & Docker Compose
- Make

### 本地开发

```bash
# 1. 克隆项目
git clone https://github.com/biovalue-ai/biovalue.git
cd biovalue

# 2. 启动依赖服务
make dev-up

# 3. 安装依赖
make build

# 4. 生成 Proto 代码
make proto

# 5. 运行 Worker
make run-worker

# 6. 运行 LLM Bridge (另一个终端)
make run-llm-bridge
```

### 环境变量

```bash
# .env.local
OPENAI_API_KEY=sk-your-api-key
DEEPSEEK_API_KEY=sk-your-deepseek-key
REDIS_PASSWORD=your-redis-password
```

## 📁 项目结构

```
BioValue-AI/
├── cmd/
│   └── worker/          # Go Worker 入口
├── internal/
│   ├── workflow/        # Temporal 工作流
│   └── activity/        # Activity 实现
├── pkg/
│   ├── config/          # 配置管理
│   ├── errors/          # 错误处理
│   ├── metrics/         # Prometheus 指标
│   ├── logging/         # 结构化日志
│   ├── tracing/         # OpenTelemetry
│   ├── cache/           # Redis 缓存
│   └── llm/             # LLM 客户端
├── llm_bridge/          # Python LLM Bridge
│   ├── server.py        # gRPC 服务
│   ├── llm_provider.py  # LLM 提供商
│   ├── mcp_client.py    # MCP 客户端
│   └── rate_limiter.py  # 限速器
├── proto/               # Proto 定义
├── skills/              # Agent Skill 定义
├── config/              # 配置文件
├── deploy/
│   ├── k8s/             # Kubernetes 部署
│   └── prometheus/      # Prometheus 配置
├── build/               # Dockerfile
├── .github/workflows/   # CI/CD
├── docker-compose.yaml
├── Makefile
└── README.md
```

## 🔧 配置说明

详见 `config/config.yaml`，支持：

- 多 LLM 提供商 (OpenAI/DeepSeek)
- 自动降级机制
- 自适应限速
- 熔断保护

## 📊 可观测性

- **Metrics**: Prometheus + Grafana (端口 3000)
- **Tracing**: OpenTelemetry + Jaeger
- **Logging**: 结构化 JSON 日志

访问：
- Temporal UI: http://localhost:8080
- Grafana: http://localhost:3000
- Prometheus: http://localhost:9091

## 🚢 部署

### Docker Compose

```bash
docker-compose up -d
```

### Kubernetes

```bash
kubectl apply -k deploy/k8s/overlays/production
```

## 📚 文档

- [CustomFramework.md](./CustomFramework.md) - 详细框架规范
- [skills/](./skills/) - Agent Skill 定义

## 📄 许可证

MIT License
